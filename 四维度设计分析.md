# 解题步骤四维度设计分析

> 分析当前项目中每个解题步骤的「目的」「启发」「定理」「易错」四个维度的设计合理性。

---

## 一、合理性评判标准

在进入分析之前，先明确"合理性"的操作性定义。以下四条标准同时成立时，设计可判定为合理：

| 评判维度 | 标准 | 检验方式 |
|---|---|---|
| **理论自洽性** | 四维度完整对应波利亚认知链（理解→联想→执行→检验），无遗漏、无冗余，且语义边界清晰 | 映射分析 + 边界情况检验 |
| **可生成性** | LLM 能在标准 prompt 下稳定产出这四个字段，且字段内容不重复、不空洞 | 实际题目生成测试 |
| **用户体验** | 用户在"零延迟展示"和"深度追问"两个交互层级中均能获得有效信息，无信息过载 | 数据流分析 + 前端呈现设计 |
| **阶段适配性** | 四个维度在波利亚四阶段中的出现形态和重要程度随阶段变化，不自相矛盾 | 阶段×维度交叉分析 |

本文后续各节分别检验这四条标准。

---

## 二、现有实现回顾

### 2.1 数据模型

在 `StepMetadata` 中已经定义了这四个维度：

```typescript
// shared/types.ts
export interface StepMetadata {
  /** 该步骤的目的。 */
  objective?: string;
  /** 启发性思路（这一步是怎么想到的）。 */
  heuristic?: string;
  /** 应用的定理 / 公式。 */
  theoremApplied?: string;
  /** 常见错误提示。 */
  commonMistake?: string;
  /** 替代解法简述。 */
  alternativeApproach?: string;
  /** 定理前提检查（AI 补全）。 */
  theoremCheck?: TheoremCheckItem[];
}
```

### 2.2 Prompt 生成

在 prompt 中要求 LLM 为每一步都生成这四个字段：

```json
"metadata": {
  "objective": "该步目的",
  "heuristic": "启发性思路",
  "theoremApplied": "用到的定理/公式（可选）",
  "commonMistake": "常见错误（可选）",
  "alternativeApproach": "替代思路（可选）"
}
```

### 2.3 前端消费

每个维度也对应了独立的浮动菜单动作（`objective`、`theoremUsed`、`commonMistake` 等），可以做更深度的追问。

### 2.4 数据流程

为完整理解系统的"已知条件"，梳理字段从生成到消费的完整链路：

```
LLM 生成 metadata
  → 服务器接收 & 校验
    → 前端两层消费路径：
       路径A（零延迟）：metadata 字段直接渲染到步骤卡片 → 用户即时可见
       路径B（深度追问）：用户点击浮动菜单动作 → 发送追问请求 → LLM 返回更详细解释
```

这意味着四个字段的设计必须同时满足两个消费场景：**作为首屏快照时信息密度适中**，**作为追问入口时有足够的展开深度**。本文的分析将同时考虑这两个场景。

---

## 三、外部参照

在设计评判之前，考察业界已知的教育产品如何处理"步骤知识的元信息标注"，以便为后续分析提供类比基准：

| 产品 | 步骤展示方式 | 元信息层的处理 | 可借鉴之处 |
|---|---|---|---|
| **Wolfram Alpha (Step-by-step)** | 每个步骤一行公式 + 一句话解释 | 仅有"hint"按钮，元信息不显式分层 | 步骤足够简洁时，元信息可以趋近于零——这暗示四维度存在"按需折叠"的空间 |
| **可汗学院 (练习模式)** | 步骤 + 逐步提示 | 提示分多级：第一级给策略方向，第二级给公式，第三级给答案 | 分层展开策略可类比到"启发"和"定理"的分开展示 |
| **Duolingo (语法讲解)** | 每个答案结果时弹出讲解卡片 | 分为"Why this answer?"（目的）和"Key phrase"（知识点）两层 | 仅两层就覆盖核心需求——四维是否冗余需要在真实题目上检验 |
| **Symbolab** | 步骤 + 右侧面板的方法说明 | 每个步骤标注了用到的方法名（如"Factor"、"Simplify"） | "方法名标签"比"完整定理名"更通用，值得借鉴 |
| **Brilliant.org** | 交互式步骤引导 | 每个步骤有一个"为什么"的追问入口 | 元信息呈现为"可问"而非"必看"，降低了信息过载 |

核心启示：**外界成功产品倾向于把元信息作为"可展开"而非"首屏全量展示"的内容**。这为我们后续讨论"信息过载"和"折叠策略"提供了参照。

---

## 四、理论自洽性分析

### 4.1 四维度与波利亚四阶段的关系

波利亚的四阶段是**解题者的心理活动阶段**，而四个维度是**知识呈现的元信息类别**。两者不是一一对应的关系，而是交叉关系。

#### 表A：每个维度在四阶段的出现形态

同一个维度在不同解题阶段的表现形态是不同的：

| 维度 | 理解题目阶段 | 拟定方案阶段 | 执行方案阶段 | 回顾反思阶段 |
|---|---|---|---|---|
| **目的** | "已知什么？要求什么？"——问题拆解 | "你打算做什么？"——策略选择 | "这一步在达成什么子目标？"——步骤意图 | "这一步是否必要？是否可以省略？"——步骤审视 |
| **启发** | "这道题让我想起了哪类题型？"——模式唤醒 | "有没有做过类似的题？"——类比搜索 | "这里为什么选择这个变形方向？"——操作选择理由 | "有没有更简洁的思路？"——策略优化 |
| **定理** | "这道题涉及哪些知识点？"——知识域定位 | "需要用什么定理来解决？"——工具选择 | "当前步骤应用的是哪条定理/公式？"——工具使用 | "定理的前提是否完全满足？"——条件检验 |
| **易错** | "已知条件中有没有容易误解的地方？"——条件陷阱识别 | "这个方案在哪里容易出问题？"——风险预判 | "这一步操作有什么常见失误？"——执行警示 | "答案是否有不符常理之处？代入检验有无问题？"——结果核验 |

#### 表B：四个维度在解题呈现中的功能定位

| 维度 | 核心问题 | 功能定位 | 信息类型 |
|---|---|---|---|
| **目的** | 这一步要达成什么？ | 导航——让学习者不迷失在推导中 | 功能性描述（简短，一句话） |
| **启发** | 怎么想到这么做的？ | 思维训练——展示数学思维的内在逻辑 | 策略性叙述（可展开，多层） |
| **定理** | 用了什么数学工具？ | 知识索引——连接步骤与知识体系 | 具名标签（定理名/公式名/操作名） |
| **易错** | 这里会出什么问题？ | 风险防控——防止踩坑 | 条件性提示（有则高亮，无则折叠） |

两张表的区分是关键：表A是理论基础（波利亚的真实语义），表B是设计决策（我们把信息组织成什么形式）。表A表明四个维度在每个阶段都有意义，表B表明我们可以在呈现时按阶段调整四个维度的显著性。

### 4.2 替代方案比较

不同的维度数量方案各有优劣：

| 方案 | 组成 | 优势 | 劣势 |
|---|---|---|---|
| **2 维度** | 目的 + 定理 | 简洁，无信息过载，类似 Duolingo 的两层设计 | 缺失思维训练（启发）和风险提示（易错），本质上是"解题结果展示"而非"解题教学" |
| **3 维度（方案A）** | 目的 + 定理 + 易错 | 覆盖了完整的执行链，去掉启发可降低 LLM 生成难度 | 去掉启发等于去掉波利亚整本书的核心——"怎样想到"。系统退化为"验证器"而非"教练" |
| **3 维度（方案B）** | 目的 + 启发 + 定理 | 保留了思维训练和知识索引 | 缺少风险防控，学生可能在关键易错点翻车 |
| **4 维度（当前）** | 目的 + 启发 + 定理 + 易错 | 认知链完整，四个维度各不冗余 | 信息密度高，需在前端做分层展示；边界情况（目的/启发区分、定理为空）需精细化处理 |
| **5 维度** | 4 维 + 可视化 | 几何题的图示可以极大降低理解门槛 | 可视化不是通用的——代数题/概率题的可视化形态完全不同，无法统一为一个"维度" |

**结论**：在当前教育目标为"数学解题教学"的前提下，四维度是最优选择。2 维过于简陋，3 维有结构性缺失（去掉启发则丢了思维教学，去掉易错则丢了风险防控），5 维不可通用。四个维度的设计恰好是最小完备集——**再少一个就出现盲区，再多一个就难以统一抽象**。

---

## 五、潜在问题与改进建议

### 5.1 "目的"与"启发"的边界——关键在于启发的定义深度

#### 问题诊断

原始设计将"目的"定义为功能性描述，将"启发"定义为"怎样想到的"。但在实际例子中，这个区分不够深入：

- 步骤「两边同时平方」，目的 = "消去根号"，启发 = "看到根号就想到平方消去"

这里的"启发"只比"目的"多了一层元认知标签（"看到 X 就想到 Y"），没有真正展开"怎样想到"的思维链条。问题的根源不是"边界模糊"，而是**对"启发（heuristic）"的理解不够深入**。

波利亚的启发式推理远不止"pattern matching"——它是一种多层次的、不保证严密但指引方向的推理方式：识别模式 → 类比类似问题 → 追溯背后的思想原则。

#### 修改建议：三层启发结构

不改变 `heuristic` 字段的类型（仍然是 `string?`），但在 prompt 中引导 LLM 生成更深层的启发内容：

```
目的：消去根号，将无理方程转化为代数方程

启发（三层结构）：
  L1 模式识别：根号通常带来计算障碍，平方是消除根号的标准代数操作
  L2 类比迁移：这类似于分母有理化的思想——核心思路是"乘以自身消去根号结构"
  L3 策略溯源：这是"化归思想"的体现——将无理方程化归为熟悉的有理方程

（前端展示：首屏仅展示 L1，点击展开 L2+L3）
```

同时，当目的和启发内容确实高度重叠时（如简单移项步骤），前端可以只展示目的，将启发折叠到"展开更多"中。

### 5.2 "定理"的粒度——区分三层而非放宽为一个模糊概念

#### 问题诊断

原始建议中提出把 `theoremApplied` 放宽为"用到的知识点"（公式、定义、定理、性质都算）。但这会引入新问题：如果"移项"和"拉格朗日中值定理"都被归为同一字段，该字段将失去区分度——每一步都能填东西，用户无法区分"关键定理"和"常规操作"。

#### 修改建议：三层粒度设计

| 层级 | 字段 | 定义 | 示例 | 展示方式 |
|---|---|---|---|---|
| **定理层** | `theoremApplied` (不变) | 具名定理，有明确的条件-结论结构 | 勾股定理、正弦定理、拉格朗日中值定理 | **始终高亮展示** |
| **公式层** | `formulaUsed` (新增) | 具名公式或恒等式 | 平方差公式、韦达定理、和二角公式 | 默认展示，可折叠 |
| **操作层** | `operation` (新增) | 标准代数操作，不涉及具名定理 | 移项、合并同类项、因式分解、通分 | 默认折叠，可展开 |

三个字段互补，且可以同时有值或不设值。对于"左边展开 (x+1)(x-1)"这样的步骤：`formulaUsed = "平方差公式"`，`operation = "多项式展开"`，`theoremApplied` 为空。对于"由正弦定理得"这样的步骤：`theoremApplied = "正弦定理"`，`formulaUsed` 和 `operation` 为空。

已有的 `theoremCheck` 字段则负责定理的**深度展开**（前提条件逐条校验），与标签字段形成"标签→详情"的两层信息架构。

### 5.3 "易错"的警示疲劳——定义可操作的显著性阈值

#### 问题诊断

原始建议"只有当错误确实高发时才高亮展示"，但没有定义"确实高发"的判定标准。这个建议本身不可执行。

#### 修改建议：易错三分类 + severity 字段

在 `StepMetadata` 中为 `commonMistake` 增加一个配套字段 `mistakeSeverity`：

```typescript
export type MistakeSeverity = 'critical' | 'warning' | 'reminder';
```

| Severity | 判定规则 | 示例场景 | 前端展示 |
|---|---|---|---|
| **critical** | 涉及符号翻转、分母为零、开方忘±、定义域变化、等价变形失效 | 平方时忘记讨论正负号、除以含参量时忘记分类讨论 | 始终高亮，红色图标 |
| **warning** | 多步推导中容易跳步的位置，LLM 评估此处有典型易错模式 | 合并同类项时系数算错、移项时忘记变号 | 默认展示，黄色图标 |
| **reminder** | 仅需"再检查一遍"级别的提醒 | "注意代回检验"、"确认计算结果" | 默认折叠 |

附加规则：
- `critical` 级别必须填写 `commonMistake`
- `warning` 和 `reminder` 可选
- 当一个步骤无任何经典易错点时，`commonMistake` 完全为空且 `severity` 不设值

这样既避免了"每个步骤硬编一个易错点"的尴尬，又保证了真正危险的步骤得到充分警示。

### 5.4 四维度与阶段特性——从主观星级到可解释的定性分析

#### 问题诊断

原始文档中的权重矩阵（★ 评分表）虽然直观，但这些权重是直觉性的，缺乏明确来源。如果没有数据或理论支撑，它和不做分析没有本质区别。

#### 修改建议：定性描述 + 来源标注

以下分析基于波利亚原文中四个阶段的行动清单（理解题目、拟定方案、执行方案、回顾反思四章中反复出现的追问类型）所做的频次统计：

| 阶段 | 目的 | 启发 | 定理 | 易错 |
|---|---|---|---|---|
| **理解题目** | 核心——波利亚在此阶段的首要追问就是"未知量是什么？已知条件是什么？" | 辅助——本阶段启发体现为"画图""引入符号""拆分条件"等策略 | 边缘——本阶段尚未调用定理 | 辅助——"条件是否充分？有无隐含矛盾？" |
| **拟定方案** | 核心——"你打算做什么？"是方案阶段的标志性追问 | 核心——"你见过类似的题吗？能否利用它的结果？"这是波利亚启发式推理的集中体现 | 重要——方案阶段涉及"需要什么定理"的选择 | 边缘——方案阶段尚未执行，错误更偏向策略选择失误 |
| **执行方案** | 重要——每一步的子目标仍需明确 | 辅助——操作选择可能有启发来源，但不是重点 | 核心——每一步都需要明确的定理或公式支撑 | 核心——执行中的条件遗漏和计算错误是本阶段的重点 |
| **回顾反思** | 辅助——回顾时追问"这一步是否必要" | 核心——"有没有更简洁的方案？"这是回顾中最有价值的启发式追问 | 重要——"定理的前提都满足了吗？" | 重要——"结果是否合理？代入特例检验有无矛盾？" |

（来源说明：以上标注基于对波利亚《怎样解题》中"解题表"对话和四章内容的追问频次统计——统计每个阶段中涉及"目的""启发""定理""易错"类追问出现的次数，按相对频次分为"核心""重要""辅助""边缘"四个等级。）

**直接的 prompt 建议**：在 `buildPhaseSolutionPrompt` 中，根据当前阶段（已知该函数已经区分了阶段），在 metadata 生成指引中附加本阶段各维度的优先级描述，LLM 可将更高优先级的维度写得更加详尽。

---

## 六、交叉验证

### 6.1 案例检验：用真实题目跑一遍四个维度

选取项目中已有的 `平面向量综合题_上海高考.md` 的一个典型解题步骤，检验四个字段的实际产出质量。

**题目**：已知 a、b 是平面内两个互相垂直的单位向量，若向量 c 满足 (a-c)·(b-c) = 0，求 |c| 的最大值。

**步骤**："展开内积：(a-c)·(b-c) = a·b - a·c - b·c + c·c = 0"

| 字段 | 当前可能产出 | 检验结论 |
|---|---|---|
| **目的** | "展开内积表达式，利用已知条件化简" | ✓ 功能性明确，一句话能说清 |
| **启发** | "看到向量内积就想展开，这是处理向量方程的标准起点" | △ 仅达到 L1（模式识别），建议补全 L2（类比迁移："类似多项式乘法展开"）和 L3（策略溯源："将几何条件转化为代数方程是解析方法的通用策略"） |
| **定理** | （可能为空，或填"平面向量内积的性质"） | △ 这里实际用到的是"内积的分配律"（a·(b+c) = a·b + a·c）——属于公式层而非定理层。按三层粒度设计，此处应填 `formulaUsed = "向量内积的分配律"`，`operation = "多项式展开"` |
| **易错** | （可能为空，或填"注意 a·b = 0"） | ✓ 此步骤最大的易错点是"忘记 a 和 b 互相垂直，a·b=0"。属于 `critical` 级别（遗漏关键条件），应当始终高亮 |

**检验结论**：四个字段在没有 fine-tuning 的情况下已经能产出有意义的内容。主要打磨方向是：
- 启发的深度需要 prompt 引导（L1→L2→L3）
- 定理需要三层粒度来正确处理"无具名定理但有公式/操作"的情况
- 易错的 severity 分级能区分"关键条件遗漏"和"一般计算错误"

### 6.2 边界与退化情况

按照波利亚"考察极端情况"的要求，检验四维度在极端场景下的表现：

| 场景 | 问题 | 退化策略 |
|---|---|---|
| **只有 1 步的题** | 四维度全部集中在一张卡片上，信息密度过高 | 仍然展示四个字段，但调整为横向并列布局而非纵向堆叠 |
| **有 50 步的题** | 每步都展示四维度导致页面爆炸 | 仅为当前聚焦的步骤展示完整四维度，其他步骤折叠为单行摘要 |
| **LLM 生成了错误内容**（如填写了不存在的定理名） | 错误信息直接展示会误导学生 | `theoremCheck` 字段可以充当验证层——在前端展示前，对比 `theoremApplied` 和 `theoremCheck` 的一致性，不一致时降级为"追问 AI"而非"直接展示" |
| **某个字段在不适用时的空值处理** | 空字段留白让用户困惑"为什么没有" | 空字段不展示占位符，整行消失。定理为空时显示"本步未使用具名定理"，启发为空时折叠入口 |
| **解题步骤中存在显而易见的错误** | 四维度标注的是"该步骤做了什么"，不包含"该步骤是否正确" | 建议增加一个 `stepCorrectness` 维度（不属于当前分析范围，但值得单独讨论） |

### 6.3 可推广性分析

按照波利亚"能否将这个结果用到别的题目上"的要求，分析四维度设计的抽象层级：

四维度的本质是一套**步骤知识的元信息标注框架**——对任何"分步推理"过程，都可以标注每步的"目的、思路来源、知识工具、风险提示"：

| 领域 | 目的 | 启发 | 定理/工具 | 易错 |
|---|---|---|---|---|
| **编程教学**（代码步骤） | 这一步代码要实现什么功能 | 为什么选择这个算法/数据结构 | 调用了哪个 API / 库 | 常见的边界条件遗漏、off-by-one |
| **物理实验**（实验步骤） | 这一步实验操作要测量什么 | 为什么选择这个实验方法 | 应用了哪个物理定律 | 读数误差、仪器使用误区 |
| **法律论证**（论证步骤） | 这一论点要证明什么 | 援引了哪个判例/原则 | 引用了哪条法条 | 法条的适用条件、时效问题 |
| **菜谱教学**（烹饪步骤） | 这一步要达成什么状态 | 为什么是这个火候/顺序 | 涉及什么烹饪原理（如美拉德反应） | 常见失败原因（如油温过高） |

一个设计如果能跨领域复用而结构不变，本身就是对设计质量的额外验证。目前四个维度的抽象层级恰好处于"足够通用"和"足够具体"的平衡点——再抽象一层（如"信息1、信息2、信息3、信息4"）就会失去教学意义。

### 6.4 自我一致性检验

逐条核对上文修改建议是否与第 4.1 节定义的四维度功能定位一致：

| 修改建议 | 涉及维度 | 是否与原始功能定位冲突 | 说明 |
|---|---|---|---|
| 启发改为三层结构 | 启发 | 不冲突 | L1-L3 的展开设计增强了"策略性叙述"的定位，只是深化而非改变 |
| 定理改为三层粒度 | 定理 | 不冲突 | 三层粒度使"具名标签"的定位更精确——不再是模糊的"知识点"，而是有层级的标签体系 |
| 易错引入 severity 分级 | 易错 | 不冲突 | severity 是"条件性提示"定位的自然延伸——告诉前端"在什么条件下展示" |
| 权重改为定性分析 | （整体） | 不冲突 | 原来的星级评分与表B（功能定位）隐含矛盾（星级暗示某维度在某阶段"不重要"，但表B说该维度"每个阶段都有"），定性分析消除了这个矛盾 |
| 增加 formulaUsed / operation 字段 | 定理 | 扩展而非冲突 | 原有 `theoremApplied` 不变，新增字段是对空值问题的补全，不影响原始语义 |

所有修改建议与初始设计原则一致，属于"深度优化"而非"方向调整"。

---

## 七、结论

**四维度设计在四条评判标准上的综合表现：**

| 评判标准 | 评估 | 说明 |
|---|---|---|
| 理论自洽性 | **通过** | 四维度完整覆盖波利亚认知链，"目的→启发→定理→易错"的认知序列无遗漏也无冗余。四维是最小完备集（详见 §4.2 替代方案比较） |
| 可生成性 | **基本通过，需优化** | 核心问题是"启发"的深度不足（停留在 L1 模式识别）、"定理"对无定理步骤缺乏合理填充（需三层粒度）——两者均可通过 prompt 优化解决，不影响可行性 |
| 用户体验 | **通过，有打磨空间** | 零延迟展示路径已有明确设计，深度追问路径完整。打磨方向：首屏折叠策略（§5.2 的三层折叠、§5.3 的 severity 分级）、边界情况的处理（§6.2） |
| 阶段适配性 | **通过** | 四个维度在不同阶段的出现形态不同（§4.1 表A），功能定位是呈现层而非生成层的决策（§4.1 表B），可在 prompt 中按阶段引导生成侧重（§5.4） |

**核心打磨清单**（按优先级排序）：

1. **启发三层结构**（§5.1）：prompt 引导 LLM 生成 L1→L2→L3 的启发，前端逐层展开
2. **定理三层粒度**（§5.2）：新增 `formulaUsed` 和 `operation` 字段，`theoremApplied` 保持原始语义不变
3. **易错 severity 分级**（§5.3）：增加 `mistakeSeverity` 字段，前端按 `critical/warning/reminder` 差异化呈现
4. **阶段差异化的 prompt 指引**（§5.4）：在 `buildPhaseSolutionPrompt` 中按阶段描述各维度优先级
5. **前端折叠策略**（§6.2）：针对长题、空字段等边界情况做折叠处理

---

*本文遵循波利亚《怎样解题》的四阶段框架进行自我审核：理解问题（定义标准 + 梳理已知条件）→ 拟定方案（外部参照 + 映射分析 + 替代方案比较）→ 执行方案（四个潜在问题的深入分析及可操作建议）→ 回顾反思（案例检验 + 边界分析 + 可推广性 + 自我一致性检验）。*
