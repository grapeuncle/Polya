# 浮动菜单产品设计分析

> 分析当前 `FloatingMenu` 组件在产品层面的设计合理性，结合各阶段内容给出修改建议。

---

## 一、当前设计回顾

### 1.1 菜单结构与数量

浮动菜单在用户点击某个解题步骤卡片后，内联展开在该步骤下方，分为两组：

| 分组 | 按钮数 | 内容 |
|------|--------|------|
| **通用操作** | 10 | 解释这一步、目的、为什么这样做、详细推导、其他方法、检验、常见错误、提问/追问、复制、标记疑难 |
| **阶段专属** | 5 | 取决于步骤所属阶段（理解/拟定/执行/回顾） |

**合计每个步骤显示 15 个按钮。**

### 1.2 四阶段专属动作

| 阶段 | 专属动作 |
|------|----------|
| 🔍 理解题目 | 拆解题干、画图/可视化、重述题目、关键信息、类似题目 |
| 🧭 拟定方案 | 思路来源、失败路径、相关题型、子目标、先猜后证 |
| ✍️ 执行方案 | 代数细节、计算检查、所用定理、参数影响、另解延续 |
| 🔭 回顾反思 | 验证答案、全部解法、推广变式、收获总结、同类练习 |

### 1.3 附加功能区

- **对话历史**：若该步骤有追问记录，以可折叠 `<details>` 展示最近 4 条消息摘要
- **追问输入框**：点击「提问/追问」后展开 textarea + 提交/取消按钮

### 1.4 交互行为

- 菜单在步骤卡片内部以内联方式渲染（`margin-top: 12px`），不脱离文档流
- 划选文本时，独立的 `SelectionToolbar` 通过 `createPortal` 定位在 `document.body`，提供「解释划选」和「提问划选」
- 点击卡片外部区域（除 `.step-card, .floating-menu, .selection-toolbar, .phase-nav, .sidebar, .mindmap, .sub-problem-nav` 外）取消选中并隐藏菜单

---

## 二、设计问题分析

### 问题 1：按钮数量过多，信息密度过高（严重）

**现状**：每个步骤卡片在选中后同时展示 15 个操作按钮。

**影响**：
- 用户面对 15 个按钮需要逐行扫描，决策成本高
- 菜单占据大量垂直空间，将后续结果卡片大幅下推，破坏阅读流
- 在移动端或窄屏场景下换行严重，几乎不可用

**产品视角**：对于一个解题辅导工具，用户在「理解题目」阶段关心的是题干拆解和关键信息提取，不需要看到「修改参数试试」或「生成同类练习」等执行/回顾阶段的选项。一刀切展示所有按钮违背了「渐进式信息披露」原则。

---

### 问题 2：通用动作与阶段专属动作的边界模糊

**现状**：部分「通用动作」在语义上与特定阶段的专属动作重叠。

| 通用动作 | 可能重叠的阶段专属动作 |
|----------|------------------------|
| 解释这一步 | 理解题目阶段 → 拆解题干（均涉及解释） |
| 为什么这样做 | 拟定方案阶段 → 思路来源（均涉及推理回溯） |
| 其他方法 | 回顾反思阶段 → 全部解法（均涉及解法对比） |
| 检验这一步 | 回顾反思阶段 → 验证答案（均涉及检验） |
| 常见错误 | 回顾反思阶段 → 收获总结（均涉及错误总结） |

**影响**：用户难以区分「解释这一步」和「拆解题干」的差异，增加了认知负担。设计上没有清晰的「通用 vs 专属」差异性说明。

---

### 问题 3：内联布局破坏页面稳定性（严重）

**现状**：浮动菜单以 `margin-top: 12px` 内联渲染在步骤卡片内部，而非 overlay/portal 方式浮于页面之上。

**实际效果**：
- 点击步骤卡片 → 菜单展开 → 卡片高度突然增加 → 页面跳动
- 向下滚动后再点击，菜单出现在视口外，需要手动回滚
- 无法实现「固定位置跟随」或「底部吸底」等更合理的定位策略

**产品视角**：名为「Floating」菜单却是静态内联，属于名实不符。一个真正的浮动菜单应该以 overlay 形式呈现，不挤占内容区域。

---

### 问题 4：与划词工具条存在功能重复和交互冲突

**现状**：系统存在两个独立的操作入口——

| 交互入口 | 触发条件 | 位置策略 | 提供动作 |
|----------|----------|----------|----------|
| FloatingMenu | 点击步骤卡片选中 | 内联，在步骤下方 | 15 个按钮 |
| SelectionToolbar | 划选步骤内文本 | Portal 到 body，fixed 定位 | 解释划选、提问划选 |

**问题**：
- 两个入口都提供「解释」和「提问」功能，但参数不同（一个带划选文本，一个不带）
- 用户可能困惑：为什么点击后出现一个菜单，划选后又出现另一个？
- 两者在视觉风格上不一致（SelectionToolbar 是迷你浮动条，FloatingMenu 是完整面板）

---

### 问题 5：缺少上下文感知的智能筛选

**现状**：无论步骤的 metadata 是否包含相关信息，所有按钮都以相同状态展示。

**具体场景**：
- 当一个步骤已经包含 `subSteps` 时，「查看详细推导」应优先使用本地数据（router 中已处理），但按钮仍与无子步骤的步骤外观相同
- 当 `metadata.commonMistake` 已有值时，「常见错误」可即时展示而不必总是调用引擎
- 当 `metadata.theoremApplied` 已有值时，「所用定理」同理

**产品视角**：有本地数据可即时展示的按钮应当有视觉区分（如徽标、不同颜色），提示用户「点击即可查看，无需等待 AI 生成」。

---

### 问题 6：缺少操作优先级和推荐机制

**现状**：15 个按钮以等权重网格排列，无任何主次区分。

**应根据使用频率和阶段特性区分优先级**：

| 优先级 | 典型动作 | 建议处理 |
|--------|----------|----------|
| 高频 | 解释、检验、提问 | 首行突出展示，更大尺寸或主色调 |
| 中频 | 目的、为什么、常见错误 | 正常展示 |
| 低频 | 标记、复制、参数影响 | 二级菜单或缩小展示 |

**产品视角**：用户在数学辅导场景中的核心诉求是「理解」和「验证」，应优先满足。目前等权排列让用户需要花时间寻找最常用的按钮。

---

### 问题 7：对话历史区域交互能力不足

**现状**：对话历史以只读 `<details>` 展示最近 4 条消息的前 80 个字符。

**缺失功能**：
- 无法点击某条历史追问重新进入该上下文
- 无法在历史基础上继续追问
- 无法展开查看完整对话内容

**产品视角**：对话历史是辅导对话的核心资产，只展示摘要而不支持交互，割裂了对话连贯性。

---

### 问题 8：阶段专属动作的部分设计不够聚焦

逐阶段审视：

#### 8.1 理解题目阶段
- 「类似题目」(similarProblem) 出现在理解阶段过早——用户还没开始解题，类比可能干扰思路
- 建议移至回顾反思阶段，作为「拓展延伸」的一部分

#### 8.2 拟定方案阶段
- 「先猜后证」(guessThenProve) 语义不够清晰，用户难以理解这个按钮的实际产出
- 建议重命名为「猜想与验证」并增加引导性 tooltip

#### 8.3 执行方案阶段
- 「修改参数试试」(tweakParams) + 「参数影响」(tweakParamsEval) 是两个紧密耦合的按钮，但 tweakParamsEval 不直接出现在菜单中（仅在 router 内部使用），导致用户点击 tweakParams 后的交互流不可见
- 建议合并为单一交互流

#### 8.4 回顾反思阶段
- 「推广变式」(generalize) 和「同类练习」(generatePractice) 语义相近但产出不同，需要更清晰的 tooltip 区分

---

### 问题 9：缺少步骤间导航和阶段跳转的快捷入口

**现状**：用户在浮动菜单中只能操作当前步骤，若想查看相邻步骤或跳转到其他阶段，必须关闭菜单后操作 PhaseNav 或滚动页面。

**产品视角**：用户在深度阅读某个步骤时，很自然地想对比前后步骤。浮动菜单中可以增加「上一步」「下一步」的迷你导航。

---

### 问题 10：缺少视觉状态反馈

**现状**：除了「标记疑难」按钮有 active 状态（橙色背景），其他按钮没有状态反馈。

**缺失的状态展示**：
- 「已查看过」的标记（用户点击某按钮后会生成 ResultCard，但无法从按钮外观知道已经执行过）
- 「正在进行中」的状态（调用引擎后的流式等待）
- 「本地可用」的提示（metadata 中已有数据）

---

## 三、上下文相关性分析

> 从「在什么阶段/状态下，展示什么内容才合理」的角度，分析当前菜单按钮与解题语境的匹配度。

### 3.1 分析框架

菜单内容的合理性需要从三个上下文维度交叉评估：

| 维度 | 说明 | 决定因素 |
|------|------|----------|
| **阶段上下文** | 用户处于 Polya 四阶段的哪一阶段 | 理解 → 拟定 → 执行 → 回顾，认知目标逐层递进 |
| **步骤上下文** | 当前步骤在解题链路中的位置和角色 | 首步/中间步/末步，是否有子步骤，是否有 metadata |
| **数据上下文** | 步骤已携带的本地数据 | subSteps、metadata.*、已有的追问记录 |

**核心原则**：菜单内容应当与用户当前的认知目标对齐——提供一个与当前阶段不相关的操作，比不提供任何操作更糟糕（增加噪音、干扰判断）。

---

### 3.2 逐阶段分析：哪些通用动作在语境下不合理

#### 3.2.1 理解题目阶段（🔍 Understand）

**用户的认知目标**：搞清楚「题目在说什么」「已知什么、求什么」。

| 通用动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 解释这一步 | ✅ 合理 | 理解阶段的核心需求 |
| 目的 | ✅ 合理 | 帮助理解这一步在整体解题中的位置 |
| 为什么这样做 | ⚠️ 超前 | 用户尚未开始推理，"为什么"针对的是决策而非理解，此时无决策可解释 |
| 详细推导 | ❌ 不合理 | 还没开始推导，无内容可展开 |
| 其他方法 | ❌ 不合理 | 连一种方法都没有，何来其他 |
| 检验 | ❌ 不合理 | 尚无结果可检验 |
| 常见错误 | ⚠️ 超前 | 可适度展示（警示性），但不应作为主要操作 |
| 提问/追问 | ✅ 合理 | 理解阶段的自然需求 |
| 复制 | ✅ 合理 | 工具性操作，与阶段无关 |
| 标记疑难 | ✅ 合理 | 理解时就可能有疑问 |

**结论**：理解阶段 10 个通用动作中有 **4 个超前或无关**（为什么、详细推导、其他方法、检验），1 个超前（常见错误）。这些按钮的存在会暗示用户「你应该考虑这些」，导致认知负荷前置。

---

#### 3.2.2 拟定方案阶段（🧭 Devise）

**用户的认知目标**：构思解题路线图，评估不同方案的可行性。

| 通用动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 解释这一步 | ✅ 合理 | 理解方案思路 |
| 目的 | ✅ 合理 | 理解方案的意图 |
| 为什么这样做 | ✅ 合理 | 拟定阶段最核心的追问——为什么选这条路 |
| 详细推导 | ⚠️ 超前 | 方案还没执行，推导言之过早 |
| 其他方法 | ✅ 合理 | 对比不同方案的优劣正是拟定阶段的核心 |
| 检验 | ⚠️ 超前 | 方案尚未执行，检验无对象 |
| 常见错误 | ✅ 合理 | 在选方案时了解常见陷阱有价值 |
| 提问/追问 | ✅ 合理 | — |
| 复制 | ✅ 合理 | — |
| 标记疑难 | ✅ 合理 | — |

**结论**：拟定阶段仅有 **2 个超前**（详细推导、检验），匹配度明显好于理解阶段。但这两个超前按钮仍构成噪音——用户看到「检验」可能误以为方案已经隐含某些需要验证的结论。

---

#### 3.2.3 执行方案阶段（✍️ Execute）

**用户的认知目标**：按方案逐步计算，确保每一步正确。

| 通用动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 解释这一步 | ✅ 合理 | 理解计算过程 |
| 目的 | ✅ 合理 | 理解这一步在整个计算链中的作用 |
| 为什么这样做 | ✅ 合理 | 理解每一步计算的选择理由 |
| 详细推导 | ✅ 合理 | 执行阶段最核心的需求——展开细节 |
| 其他方法 | ⚠️ 超前 | 如果当前方案正确运行中，切换思路会打断流 |
| 检验 | ✅ 合理 | 验证计算结果正确性 |
| 常见错误 | ✅ 合理 | 避免在当前计算中踩坑 |
| 提问/追问 | ✅ 合理 | — |
| 复制 | ✅ 合理 | — |
| 标记疑难 | ✅ 合理 | — |

**结论**：执行阶段匹配度最高，仅 **1 个不相关**（其他方法）。这说明当前通用动作集在语义上偏向「执行」阶段——但这恰恰反衬出它在其他阶段的适应性问题。

---

#### 3.2.4 回顾反思阶段（🔭 Review）

**用户的认知目标**：验证答案、总结方法、拓展迁移。

| 通用动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 解释这一步 | ⚠️ 冗余 | 回顾阶段用户已理解过程，重新解释价值有限 |
| 目的 | ⚠️ 冗余 | 同理，目的在理解/拟定阶段已明确 |
| 为什么这样做 | ⚠️ 冗余 | 执行阶段已解释过推理 |
| 详细推导 | ⚠️ 冗余 | 推导在此阶段不是主要关注点 |
| 其他方法 | ✅ 合理 | 回顾阶段寻找替代解法是核心需求 |
| 检验 | ✅ 合理 | 验证是回顾的第一要务 |
| 常见错误 | ✅ 合理 | 总结错误是回顾的重要内容 |
| 提问/追问 | ✅ 合理 | — |
| 复制 | ✅ 合理 | — |
| 标记疑难 | ✅ 合理 | — |

**结论**：回顾阶段出现 **4 个冗余**（解释、目的、为什么、详细推导）——这些操作在前三个阶段已经可以被覆盖，回顾阶段重提会造成「还需要再理解一遍吗」的困惑。

---

### 3.3 逐阶段分析：阶段专属动作的语境合理性

#### 3.3.1 理解题目阶段

| 专属动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 拆解题干 | ✅ 核心 | 理解阶段最需要的操作 |
| 画图/可视化 | ✅ 核心 | 几何/物理题的理解关键 |
| 重述题目 | ✅ 合理 | 用自己的话复述以检验理解 |
| 关键信息 | ✅ 核心 | 提取已知条件和求解目标 |
| 类似题目 | ⚠️ 有争议 | 见下文详细分析 |

**「类似题目」的争议分析**：

- **正方**：类比是理解抽象问题的重要手段，看到一个类似的已解决问题有助于建立题型认知
- **反方**：在用户尚未充分理解当前题目时，展示另一个题目可能造成干扰——用户需要同时处理两个问题的信息
- **数据视角**：如果没有 metadata 表明当前题目有强关联的同类题，AI 生成的类比可能质量不高

**建议**：将「类似题目」拆分为两个层面——

```
理解阶段：结构类比 → 只展示题目结构/模式的相似性（如"都是相遇问题"），不展示完整题目
回顾阶段：完整类比 → 展示完整相似题目，包括解法和答案
```

---

#### 3.3.2 拟定方案阶段

| 专属动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 思路来源 | ✅ 核心 | 理解方案的灵感来源和合理性 |
| 失败路径 | ✅ 核心 | 了解常见的错误思路，避免走弯路 |
| 相关题型 | ✅ 合理 | 辅助方案迁移 |
| 子目标 | ✅ 核心 | 将大方案拆解为可管理的子步骤 |
| 先猜后证 | ⚠️ 位置不当 | 猜测-验证是执行策略，不是拟定策略 |

**「先猜后证」的位置问题**：这个动作描述的是「先猜测一个中间结果，然后通过计算验证」——这实际上是**执行**阶段的一种策略（猜一个值代进去试试）。在拟定阶段，用户需要的是「确定方案」，而非「在方案内猜测」。将其放在拟定阶段会混淆「选择方案」和「在方案内试错」两个层次。

---

#### 3.3.3 执行方案阶段

| 专属动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 代数细节 | ✅ 核心 | 展开代数变换的具体步骤 |
| 计算检查 | ✅ 核心 | 验证计算正确性 |
| 所用定理 | ✅ 核心 | 理解每一步依据的数学原理 |
| 参数影响 | ⚠️ 时机不当 | 见下文分析 |
| 另解延续 | ✅ 合理 | 当前方案某步分叉出其他路径 |

**「参数影响」的时机问题**：分析「如果某个参数变化，结果会怎样」是一种**敏感性分析**，更适合在回顾反思阶段进行——它假设用户已经完成了解题、得到了最终结果，然后回过来探讨「如果题目条件不同会怎样」。在执行阶段中途做参数影响分析，可能打断计算流。

---

#### 3.3.4 回顾反思阶段

| 专属动作 | 语境合理性 | 分析 |
|----------|-----------|------|
| 验证答案 | ✅ 核心 | 回顾的第一要务 |
| 全部解法 | ✅ 核心 | 探索其他可能的解法 |
| 推广变式 | ✅ 核心 | 从一道题推广到一类题 |
| 收获总结 | ✅ 核心 | 提炼方法论和知识点 |
| 同类练习 | ✅ 核心 | 提供巩固练习 |

**回顾阶段的专属动作设计最为合理**，5 个动作全部与回顾语境高度匹配，无争议项。这也说明回顾阶段的动作设计是四个阶段中最成熟的。

---

### 3.4 步骤位置上下文分析

同一个阶段的菜单内容，在不同步骤位置上应有不同偏重：

| 步骤位置 | 语境特征 | 菜单应偏重 |
|----------|----------|-----------|
| **首步**（阶段入口） | 用户刚进入新阶段，需要建立全局认知 | 偏重"目的"、"为什么"、阶段概述类操作 |
| **中间步** | 用户已进入执行节奏 | 偏重"详细推导"、"检验"、"常见错误"等具体操作 |
| **末步**（阶段出口） | 用户即将进入下一阶段 | 偏重"总结"、"过渡提示"，可预告下一阶段的操作 |
| **唯一一步** | 该阶段仅有 1 步 | 综合所有本阶段相关操作，不需省略 |

**当前问题**：菜单对所有位置的步骤完全一致，忽略了步骤序号带来的语境变化。例如，理解阶段的第 3 步（已接近理解完成）仍展示「拆解题干」作为主要专属动作——此时用户可能更需要「关键信息汇总」而非重新拆解。

---

### 3.5 数据上下文分析

步骤已携带的数据应当影响菜单的展示策略：

| 数据条件 | 应有行为 | 当前行为 | 差距 |
|----------|----------|----------|------|
| `step.subSteps` 非空 | 「详细推导」应优先展示，且可零延迟展开 | 与 subSteps 为空的步骤相同 | 浪费本地数据优势 |
| `metadata.objective` 有值 | 「目的」应显示缓存命中标记 | 无标记 | 用户不知道可即时查看 |
| `metadata.commonMistake` 有值 | 「常见错误」可直接展示 | 仍调用引擎 | 重复生成已有内容 |
| `metadata.theoremApplied` 有值 | 「所用定理」可直接展示 | 仍调用引擎 | 同上 |
| 该步骤已有追问历史 | 应展示对话摘要和继续入口 | 以只读 details 展示最近 4 条 | 用户无法交互 |
| 该 action 已有 done 结果 | 按钮应显示「已有结果」标记 | 无标识 | 用户可能重复执行 |

**核心原则**：菜单应当是一个「智能面板」而不仅是「按钮列表」——当数据已在本地时，菜单的角色应从「触发引擎调用」转变为「直接展示结果入口」。

---

### 3.6 综合评估：上下文匹配度矩阵

|  | 理解 🔍 | 拟定 🧭 | 执行 ✍️ | 回顾 🔭 |
|--|:--:|:--:|:--:|:--:|
| **通用动作匹配度** | 5/10 ✅ | 8/10 ✅ | 9/10 ✅ | 6/10 ✅ |
| **专属动作匹配度** | 4/5 ✅ | 4/5 ✅ | 4/5 ✅ | 5/5 ✅ |
| **步骤位置感知** | ❌ | ❌ | ❌ | ❌ |
| **数据状态感知** | ❌ | ❌ | ❌ | ❌ |
| **综合可用性** | ⚠️ 中 | ✅ 良 | ✅ 良 | ⚠️ 中 |

**关键发现**：
1. **执行阶段是唯一几乎全匹配的阶段**——这说明当前菜单设计在潜意识中围绕「执行」阶段构建
2. **理解阶段和回顾阶段各有约一半的通用动作不在语境中**——这是菜单显得「臃肿」的根本原因，不是按钮太多，而是无关按钮太多
3. **步骤位置感知和数据状态感知完全缺失**——菜单是静态的，不随解题进程演进

---

### 3.7 上下文相关的改进方向

基于以上分析，建议从以下方向优化菜单内容的上下文相关性：

#### 方向 1：按阶段裁剪通用动作

```
理解阶段只需保留：解释、目的、提问、复制、标记（5 个）
拟定阶段只需保留：解释、目的、为什么、其他方法、常见错误、提问、复制、标记（8 个）
执行阶段只需保留：解释、目的、为什么、详细推导、检验、常见错误、提问、复制、标记（9 个）
回顾阶段只需保留：其他方法、检验、常见错误、提问、复制、标记（6 个）
```

**收益**：每个阶段的通用动作从 10 个缩减为 5-9 个，消除不相关的认知噪音。

#### 方向 2：步骤位置动态调整

```typescript
// 伪代码示例
function getMenuForStep(step, phase, stepIndex, totalSteps) {
  const actions = getBaseActionsForPhase(phase);
  
  if (stepIndex === 0) {
    actions.prioritize(['purpose', 'overview']);  // 首步偏重理解
  }
  if (stepIndex === totalSteps - 1) {
    actions.addHint('即将进入下一阶段', nextPhaseHint);  // 末步提示过渡
  }
  if (step.subSteps?.length) {
    actions.promote('详细推导', { badge: step.subSteps.length });  // 有子步骤时提升优先级
  }
  
  return actions;
}
```

#### 方向 3：数据驱动的按钮状态切换

| 数据条件 | 按钮状态 |
|----------|----------|
| 无本地数据 | 标准按钮 → 点击调用引擎 |
| 有本地数据 | 高亮按钮 + 徽标 → 点击即时展示 |
| 已有生成结果 | ✅ 标记 → 点击查看已有结果 |
| 引擎生成中 | ⏳ 加载动画 → 点击取消或等待 |

---

## 四、修改建议

> 以下建议综合了交互设计分析（第二章）和上下文相关性分析（第三章）的结论。

### 建议 1：缩小按钮数量，引入二级分组（高优先级）

将 15 个按钮压缩为 **5-7 个一级按钮** + 可展开的二级菜单。

**推荐方案**：

```
一级（始终可见，横向排列）：
┌─────────┬──────┬──────┬──────┬─────────┐
│ 💬 解释  │ ✅ 检验│ ❓ 提问│ ⚑ 标记│ ··· 更多│
└─────────┴──────┴──────┴──────┴─────────┘

二级（点击「更多」或向下展开）：
┌────────────────────────────────────────────┐
│ 【理解类】                                  │
│  目的  |  为什么这样做  |  常见错误         │
│ 【推导类】                                  │
│  详细推导  |  其他方法  |  代数细节         │
│ 【阶段专属】                                │
│  拆解题干  |  可视化  |  重述题目  | ...    │
└────────────────────────────────────────────┘
```

**收益**：
- 一级菜单只展示 5 个最高频操作，视觉清爽
- 二级菜单按语义分组（理解类/推导类/阶段专属），降低扫描成本
- 保持所有功能可达性，不删减任何动作

---

### 建议 2：改为真正的浮动面板（高优先级）

将菜单渲染方式从内联改为 `createPortal` 到 `document.body`，定位在选中步骤附近。

**具体方案**：
- 使用 `getBoundingClientRect()` 获取步骤卡片位置
- 菜单以 `position: fixed` 定位在卡片右侧或下方
- 使用 `ResizeObserver` 监听页面滚动和窗口大小变化，自动调整位置
- 在步骤卡片上保留一个视觉锚点（如高亮边框或小箭头），指示菜单来源

**收益**：
- 不破坏页面布局，无跳动
- 菜单始终可见，不受滚动影响
- 符合「浮动」语义

---

### 建议 3：合并 FloatingMenu 和 SelectionToolbar（中优先级）

将划词工具条作为浮动菜单的一个子模式，而非独立组件。

**方案**：
- 当用户划选文本时，浮动菜单自动切换到「划选模式」
- 在划选模式下，「解释」和「提问」按钮自动携带选中文本
- 统一视觉风格和交互逻辑

**收益**：
- 减少用户需要理解的交互模式数量
- 消除功能重复
- 代码可维护性提升

---

### 建议 4：增加上下文感知的按钮状态（中优先级）

根据步骤 metadata 动态调整按钮外观和行为。

| 条件 | 按钮变化 |
|------|----------|
| `step.subSteps` 非空 | 「详细推导」按钮显示子步骤数量徽标（如「展开 3 步」） |
| `metadata.objective` 非空 | 「目的」按钮显示本地可用标记（如绿色圆点） |
| `metadata.commonMistake` 非空 | 「常见错误」按钮显示本地可用标记 |
| `metadata.theoremApplied` 非空 | 「所用定理」按钮显示本地可用标记 |
| 已有 done 状态的同 action 结果 | 按钮旁显示「✓ 已有结果」 |

**收益**：
- 用户能立即知道哪些操作可以零延迟获得结果
- 减少不必要的引擎调用，节省 API 成本

---

### 建议 5：增加步骤间迷你导航（低优先级）

在浮动菜单顶部或底部增加简洁的上/下一步导航。

```
┌─────────────────────────────────────┐
│  ◀ 上一步（第 2 步）  │  下一步（第 4 步） ▶ │
├─────────────────────────────────────┤
│  [操作按钮区域]                       │
└─────────────────────────────────────┘
```

**收益**：
- 用户可以在深度阅读时快速切换步骤
- 减少关闭菜单→滚动→点击的操作链路

---

### 建议 6：调整阶段专属动作归属（低优先级）

| 动作 | 当前阶段 | 建议阶段 | 理由 |
|------|----------|----------|------|
| 类似题目 (similarProblem) | 理解题目 | 回顾反思，或拆分展示 | 理解阶段只展示「结构类比」（如"都是相遇问题"），回顾阶段展示完整题目和解答 |
| 先猜后证 (guessThenProve) | 拟定方案 | 执行方案 | 猜测-验证是执行中的试错策略，非方案拟定概念 |
| 参数影响 (tweakParamsEval) | 执行方案 | 回顾反思 | 参数敏感性分析应在得到最终答案后进行，执行中途做会打断计算流 |

或者，保留 `similarProblem` 在理解阶段，但修改其作用——在理解阶段展示「结构类比」而非「完整题目」，帮助建立题目类型认知而非直接做题。

---

### 建议 7：优化对话历史交互（低优先级）

将对话历史从只读摘要升级为可交互组件：
- 点击某条历史消息展开完整对话
- 提供「继续追问」入口，自动回填上下文
- 显示追问状态（进行中/已完成）

---

### 建议 8：增加快捷键支持

为高频操作绑定键盘快捷键：
- `E` → 解释
- `V` → 检验
- `A` → 提问
- `Esc` → 关闭菜单

---

## 五、优先级排序总结

| 优先级 | 建议 | 预期工作量 | 用户影响 |
|--------|------|-----------|----------|
| 🔴 高 | 建议 1：缩小按钮数量 + 按阶段裁剪通用动作 | 中 | 大幅降低认知负荷，消除语境无关按钮 |
| 🔴 高 | 建议 2：改为真正浮动面板 | 中 | 解决页面跳动核心痛点 |
| 🟡 中 | 建议 3：合并两个工具条 | 中 | 统一交互心智模型 |
| 🟡 中 | 建议 4：上下文感知状态 + 数据驱动按钮切换 | 小 | 优化信息获取效率，节省引擎调用 |
| 🟡 中 | 新增：步骤位置动态感知 | 中 | 首末步骤的菜单更有针对性 |
| 🟢 低 | 建议 5：步骤间导航 | 小 | 提升深度阅读体验 |
| 🟢 低 | 建议 6：调整动作归属（含先猜后证、参数影响移位） | 小 | 语义更合理，消除阶段错位 |
| 🟢 低 | 建议 7：对话历史交互 | 中 | 增强对话连贯性 |
| 🟢 低 | 建议 8：快捷键 | 小 | 提升效率 |

---

## 六、推荐的实施路线

**第一阶段（MVP 优化）**：建议 1 + 建议 2
- 将 15 个按钮压缩为一级 5 个 + 二级分组，并按阶段裁剪通用动作（上下文方向 1）
- 将菜单改为 Portal 浮动定位
- 这是用户感知最强的两项改进

**第二阶段（体验打磨）**：建议 3 + 建议 4 + 新增：步骤位置感知
- 统一浮动菜单和划词工具条
- 增加按钮状态标识（数据驱动按钮切换）
- 首步/末步的菜单内容根据步骤位置动态调整

**第三阶段（深度优化）**：建议 5-8
- 导航增强、动作归属调整、对话历史交互、快捷键
